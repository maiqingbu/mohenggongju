# 状态机动态审批 — 完整设计文档

## 一、核心理念

将当前的"线性流水线"改造为**带条件循环的引导式状态机**。

- 不是一口气跑完所有步骤，而是**按需检测、缺什么补什么**
- 每个审批卡片以消息形式展现在 Agent 面板中
- 状态字样随进度实时更新
- 审批卡片必须接通 AiModal 精准按钮，不能只跳转面板

---

## 二、状态定义

| 状态 | 含义 |
|------|------|
| `idle` | 空闲，无工作流运行 |
| `running` | 正在执行某步骤（AI生成/系统检测） |
| `awaiting` | 某步骤完成，等待用户审批 |
| `paused` | 用户主动暂停 |
| `done` | 全部完成 |
| `aborted` | 中止或出错 |

---

## 三、作品全生命周期流程

```
灵感火花（纯用户操作，agent不参与）
    │
    ▼ 灵感落库（作品创建）
    │
    ▼
阶段1：作品设定检测 → 缺失 → 审批卡片 → 用户补充 → 重新检测 → 全部齐全
    │
    ▼
阶段2：信息设定检测 → 缺失 → 审批卡片 → 用户补充/AI生成 → 重新检测 → 全部齐全
    │
    ▼
阶段3：大纲层级检测 → 总纲 → 卷纲 → 章纲（按层级依赖，必须依次完成）
    │
    ▼
阶段4：开篇正文（黄金三章）→ 用户手动创作 → 正文落库
    │
    ▼
阶段5：设定更新 → AI分析已写内容 → 审批卡片 → 更新角色/设定/伏笔/物品
    │
    ▼
阶段6：续写循环（动态递进检测，写到哪检测到哪）
    │
    ▼
完结
```

---

## 四、阶段详解

### 阶段1：作品设定检测

检测项：
- 书名
- 类型/题材
- 简介/一句话概述
- 核心卖点
- 目标读者
- 金手指/核心设定
- ...其他设定面板小板块

流程：
```
检测所有设定项
    │
    ▼
有缺失？
    ├── 有 → 审批卡片："作品设定补充"
    │        列出所有缺失项，每项有"去补充"按钮
    │        → 接通AiModal，精准跳转到对应板块
    │        → 用户补充完成 → 回到检测重新扫描
    │
    └── 没有 → 进入阶段2
```

### 阶段2：信息设定检测

检测项：
- 主要角色
- 世界观/背景
- 伏笔/悬念
- 关键物品
- 情节线/主线

流程：
```
检测所有信息项
    │
    ▼
有缺失？
    ├── 有 → 审批卡片："信息设定补充"
    │        AI自动生成设定 或 列出缺失项让用户补充
    │        → 接通AiModal，精准跳转到对应板块
    │        → 用户完成 → 回到检测重新扫描
    │
    └── 没有 → 进入阶段3
```

### 阶段3：大纲层级检测

**层级依赖强制**：总纲 → 卷纲 → 章纲，必须按顺序完成。

流程：
```
1. 有总纲吗？
   没有 → 审批卡片："生成总纲"
          → 接通AiModal总纲生成
          → 完成后状态更新："总纲已完成 ✓"
          → 继续检测

2. 有卷纲吗？（至少第一卷）
   没有 → 审批卡片："生成第一卷卷纲"
          → 接通AiModal卷纲生成
          → 完成后状态更新："第一卷卷纲已完成 ✓"
          → 继续检测

3. 有章纲吗？（至少开篇3章）
   没有 → 审批卡片："生成开篇章纲"
          → 接通AiModal章纲生成
          → 完成后状态更新："第1-3章章纲已完成 ✓"
          → 继续检测

全部齐全 → 进入阶段4
```

### 阶段4：开篇正文（黄金三章）

流程：
```
前置检查：
- 开篇3章章纲必须存在
- 所有设定必须齐全

审批卡片："开始创作黄金三章"
→ 用户手动完成黄金三章正文
→ 正文落库
→ 进入阶段5
```

### 阶段5：设定更新

流程：
```
AI分析已写内容，检查：
- 是否出现新角色？ → 更新角色面板
- 是否出现新设定？ → 更新世界观
- 伏笔是否已埋下？ → 更新伏笔面板
- 物品是否已记录？ → 更新物品面板

审批卡片："设定更新审查"
→ 列出所有需要更新的项
→ 接通AiModal，精准跳转到对应板块
→ 用户确认/补充完成
→ 进入阶段6
```

### 阶段6：续写循环（动态递进）

**核心逻辑**：每一章续写前，动态检测该章是否具备创作条件。

流程：
```
用户要写第 N 章
    │
    ▼
检测1：第 N 章属于哪一卷？
    │
    ▼
检测2：该卷有卷纲吗？
    │
    ├── 没有 → 审批卡片："生成第X卷卷纲"
    │          → 接通AiModal卷纲生成
    │          → 完成后状态更新："第X卷卷纲已完成 ✓"
    │          → 继续检测章纲
    │
    └── 有 → 继续检测章纲
              │
              ▼
检测3：第 N 章有章纲吗？
    │
    ├── 没有 → 审批卡片："生成第N章章纲"
    │          → 接通AiModal章纲生成
    │          → 完成后状态更新："第N章章纲已完成 ✓"
    │          → 继续
    │
    └── 有 → 继续
              │
              ▼
检测4：设定是否齐全？（可选，快速扫描）
    │
    ▼
全部就绪 → 执行续写工作流
    │
    ▼
正文落库
    │
    ▼
设定更新（同阶段5）
    │
    ▼
还有下一章？ → 是 → 回到循环开始
            → 否 → 作品完结
```

---

## 五、具体例子

假设：用户设定每卷50章

```
作品创建
  │
  ▼
设定检测 ✓ → 信息检测 ✓ → 大纲检测
  │
  ▼
总纲 ✓ → 第一卷卷纲 ✓ → 第1-3章章纲 ✓
  │
  ▼
黄金三章（第1-3章正文）✓
  │
  ▼
设定更新 ✓
  │
  ▼
续写第4章 → 有章纲 → 直接续写 ✓
  │
  ▼
续写第5章 → 没有章纲
  │
  ▼
审批卡片："第5章缺少章纲，需要先生成"
  → 用户确认 → AiModal生成章纲 ✓
  → 状态更新："第5章章纲已完成 ✓"
  → 自动续写第5章 ✓
  │
  ▼
...（重复，每写一章检测下一章是否有章纲）
  │
  ▼
续写第50章 → 有章纲 → 续写 ✓
  │
  ▼
续写第51章 → 属于第二卷
  │
  ▼
第二卷有卷纲吗？ → 没有
  │
  ▼
审批卡片："第一卷已完成，需要先生成第二卷卷纲"
  → 用户确认 → AiModal生成第二卷卷纲 ✓
  → 状态更新："第二卷卷纲已完成 ✓"
  │
  ▼
审批卡片："生成第51章章纲"
  → 用户确认 → AiModal生成第51章章纲 ✓
  → 状态更新："第51章章纲已完成 ✓"
  │
  ▼
自动续写第51章 ✓
  │
  ▼
...（继续循环直到完结）
```

---

## 六、状态字样动态更新

Agent 面板中的状态标签随进度实时变化：

| 阶段 | 未完成 | 进行中 | 已完成 |
|------|--------|--------|--------|
| 作品设定 | 📝 去补充 | ⏳ 补充中... | ✅ 作品设定已完成 |
| 信息设定 | 📝 去补充 | ⏳ 补充中... | ✅ 信息设定已完成 |
| 总纲 | 📝 生成总纲 | ⏳ 生成中... | ✅ 总纲已完成 |
| 第X卷卷纲 | 📝 生成卷纲 | ⏳ 生成中... | ✅ 第X卷卷纲已完成 |
| 第N章章纲 | 📝 生成章纲 | ⏳ 生成中... | ✅ 第N章章纲已完成 |
| 黄金三章 | 📝 开始创作 | ⏳ 创作中... | ✅ 黄金三章已完成 |
| 设定更新 | 📝 更新设定 | ⏳ 更新中... | ✅ 设定已更新 |
| 第N章正文 | 📝 继续创作 | ⏳ 创作中... | ✅ 第N章已完成（X字） |

---

## 七、审批卡片设计

### 消息化展示

每个审批卡片以消息形式展现在 Agent 面板聊天流中：

```
[系统消息] 🔄 正在检测作品设定...
[审批卡片] 📋 作品设定补充 — 检测到3项缺失
            · 书名：未设置     [去补充]
            · 简介：未设置     [去补充]
            · 金手指：未设置   [去补充]
            [全部补充完成，继续]
[系统消息] ✅ 作品设定已完成
```

### 精准接通 AiModal

审批卡片上的每个操作按钮必须精准接通 AiModal：

| 按钮 | 接通目标 |
|------|----------|
| [生成书名] | AiModal → 预填"生成书名"任务 → 完成回写书名字段 |
| [生成简介] | AiModal → 预填"生成简介"任务 → 完成回写简介字段 |
| [生成角色] | AiModal → 预填"生成角色"任务 → 完成回写角色面板 |
| [生成总纲] | AiModal → 预填"生成总纲"任务 → 完成回写总纲面板 |
| [生成卷纲] | AiModal → 预填"生成卷纲"任务 → 完成回写卷纲面板 |
| [生成章纲] | AiModal → 预填"生成章纲"任务 → 完成回写章纲面板 |
| [去补充] | AiModal → 预填对应补充任务 → 完成回写对应字段 |

---

## 八、Runner 状态机改造

### 当前问题

| 问题 | 说明 |
|------|------|
| 线性遍历 | `for (i = 0; i < steps.length; i++)` 按数组顺序走，无法条件跳转 |
| next 字段未使用 | WorkflowStep 定义了 next 但 run() 没用它 |
| 无自动重试 | LLM 调用失败直接挂掉 |
| redo 逻辑重复 | run() 和 resumeFromRestore() 各写了一份 |
| 上下文丢失静默 | resolveVariable 找不到变量时悄悄跳过 |

### 改造方案

**改动1：run() 方法改为步骤图路由**

```typescript
// 当前：线性遍历
for (let i = 0; i < steps.length; i++) { ... }

// 改造：步骤图 + 条件路由
const stepMap = new Map(steps.map(s => [s.id, s]))
let currentStepId = steps[0].id

while (currentStepId) {
  const step = stepMap.get(currentStepId)
  if (!step) break
  
  const output = await executeStep(agent, step)
  const decision = await handleApproval(step, output)
  
  // 条件路由：根据输出决定下一步
  if (step.condition) {
    currentStepId = step.condition(output)
  } else {
    currentStepId = step.next
  }
}
```

**改动2：WorkflowStep 增加 condition 字段**

```typescript
interface WorkflowStep {
  // ... 现有字段
  condition?: (output: string) => string | null  // 条件路由函数
}
```

**改动3：LLM 调用自动重试**

```typescript
async function executeStepWithRetry(agent, step, maxRetries = 3) {
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      return await executeStep(agent, step)
    } catch (e) {
      if (isRetryableError(e) && attempt < maxRetries - 1) {
        await sleep(1000 * Math.pow(2, attempt))  // 指数退避
        continue
      }
      throw e
    }
  }
}
```

**改动4：合并 redo 逻辑**

```typescript
// 提取公共 redo 处理方法
private async handleRedoLoop(
  step: WorkflowStep,
  agent: AgentSpec,
  mode: WorkMode,
  initialDecision: Decision
): Promise<'approved' | 'skipped' | 'aborted'> {
  // 统一的 redo 循环逻辑
  // run() 和 resumeFromRestore() 共用此方法
}
```

**改动5：新增续写前置检测步骤**

```typescript
// 新增 preflight_check agent
const preflightCheckAgent: AgentSpec = {
  id: 'preflight_check',
  name: '续写前置检测',
  localExecute: async (inputs, ctx) => {
    const chapterNo = parseInt(inputs.chapterNo)
    const workId = ctx.workId as number
    
    const result = preflightCheck(chapterNo, workId)
    
    if (!result.ready) {
      // 返回缺失项，由 runner 推送审批卡片
      return JSON.stringify({
        ready: false,
        missingItems: result.missingItems
      })
    }
    
    return JSON.stringify({ ready: true })
  },
  // ...
}
```

---

## 九、新增文件清单

| 文件 | 用途 |
|------|------|
| `src/agents/workflows/lifecycle.ts` | 作品全生命周期工作流构建器 |
| `src/agents/steps/preflightCheck.ts` | 续写前置检测 agent |
| `src/agents/steps/settingDetector.ts` | 设定缺失检测 agent |
| `src/agents/steps/outlineDetector.ts` | 大纲层级检测 agent |
| `src/composables/useLifecycleWorkflow.ts` | 生命周期工作流 composable |
| `src/composables/useProjectProgress.ts` | 项目进度状态管理 |

## 十、修改文件清单

| 文件 | 改动 |
|------|------|
| `src/agents/runner.ts` | run() 改为步骤图路由、新增 condition 支持、合并 redo 逻辑、LLM 重试 |
| `src/agents/types.ts` | WorkflowStep 增加 condition 字段 |
| `src/components/AgentPanel.vue` | 接入生命周期工作流、审批卡片消息化、状态字样更新、接通 AiModal |
| `src/agents/workflows/continueChapter.ts` | 增加 preflight_check 步骤 |
| `src/agents/steps/commitWrite.ts` | 落库后触发设定更新钩子 |

---

## 十一、续写循环伪代码

```typescript
async function continueWriting(workId: number) {
  const nextChapterNo = getNextChapterNo(workId)
  
  // 1. 前置检测
  const check = preflightCheck(nextChapterNo, workId)
  
  if (!check.ready) {
    for (const item of check.missingItems) {
      if (item.type === 'volume_outline') {
        await pushApprovalCard({
          title: `生成第${item.volumeNo}卷卷纲`,
          description: `第${nextChapterNo}章属于第${item.volumeNo}卷，需要先生成卷纲`,
          action: 'generate_volume_outline',
          targetVolume: item.volumeNo,
        })
        updateStatus(`第${item.volumeNo}卷卷纲已完成 ✓`)
      }
      
      if (item.type === 'chapter_outline') {
        await pushApprovalCard({
          title: `生成第${nextChapterNo}章章纲`,
          description: `续写前需要先生成本章章纲`,
          action: 'generate_chapter_outline',
          targetChapter: nextChapterNo,
        })
        updateStatus(`第${nextChapterNo}章章纲已完成 ✓`)
      }
    }
    // 缺失项补齐后自动回到续写
    return continueWriting(workId)
  }
  
  // 2. 一切就绪，开始续写
  await runContinueWorkflow(nextChapterNo)
  
  // 3. 正文落库后，触发设定更新
  await runSettingUpdate(nextChapterNo)
  
  // 4. 继续下一章
  return continueWriting(workId)
}
```

---

## 十二、续写循环三重检测

每一章续写前：

```
检测1：这一卷有卷纲吗？  → 没有 → 生成卷纲审批卡
检测2：这一章有章纲吗？  → 没有 → 生成章纲审批卡
检测3：所有设定齐全吗？  → 缺失 → 设定补充审批卡

全部通过 → 开始续写 → 正文落库 → 设定更新 → 下一章
```

---

## 十三、设计亮点

1. **动态递进**：不是一次性生成所有大纲，而是按需生成，写到哪检测到哪
2. **层级依赖强制**：总纲 → 卷纲 → 章纲，必须按顺序
3. **审批卡片消息化**：所有审批以消息形式展示在 Agent 面板
4. **状态字样实时更新**：面板标签随进度变化
5. **精准接通 AiModal**：不只跳转面板，必须精准到按钮
6. **落库触发更新**：正文落库后自动触发设定更新检测
7. **自动串联**：缺失项补齐后自动回到续写流程，无需手动操作

---

## 十四、Runner 改造后的状态转换

```
idle
  │
  ▼ run(steps, mode)
running
  │
  ├── 执行步骤
  │     │
  │     ▼
  │   shouldBlock?
  │     │
  │     ├── 否 → 直接 writeBack → 下一步（仍 running）
  │     │
  │     └── 是 → buildApprovalCard
  │               │
  │               ▼
  │           awaiting
  │               │
  │               ▼ 用户决策
  │           ┌───┼───┬───────┐
  │           ▼   ▼   ▼       ▼
  │        approve redo skip  abort
  │           │   │   │       │
  │           │   │   │       ▼
  │           │   │   │     aborted
  │           │   │   ▼
  │           │   │  下一步（仍 running）
  │           │   ▼
  │           │  重新执行当前步骤（仍 running）
  │           ▼
  │        writeBack → 下一步（仍 running）
  │
  ▼ 所有步骤完成
done
```
